name: Keep Streamlit Alive

on:
  schedule:
    # Her gün Türkiye saatiyle sabah 09:00'da (06:00 UTC) otomatik çalışır
    - cron: '0 6 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://plakradar.streamlit.app
 
